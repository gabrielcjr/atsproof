services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ats_matcher
    restart: unless-stopped
    ports:
      - "80:80"
    env_file:
      - .env
    environment:
      - PYTHONUNBUFFERED=1
