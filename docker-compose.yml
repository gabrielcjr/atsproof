services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ats_matcher
    restart: unless-stopped
    ports:
      - "127.0.0.1:8000:8000"
    env_file:
      - .env
    environment:
      - PYTHONUNBUFFERED=1

